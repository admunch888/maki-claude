min_maki_version = "0.5.3"

# fs_read and fs_write install the provider script into the config directory,
# run starts the proxy job and calls the script for /claude.
[permissions]
fs_read = true
fs_write = true
run = true
